"use strict";

var __db__ = require('__driver/db'),
	__async__ = require('__driver/myasync').async,
	__notifications__ = require("__driver/notifications"),
	__cache__ = require("__driver/cache"),
	__sectionStarting__ = require("__driver/post/section/start.js"),
	__sectionFinishing__ = require("__driver/post/section/finish.js");

module.exports = {
	exec : function(parms){
		if(parms.url_data.indexOf("start") != -1){
			__sectionStarting__.exec(parms);
		}else if(parms.url_data.indexOf("finish") != -1){
			__sectionFinishing__.exec(parms);
		}else{
		}
	}
}